# DataSource Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Root-123
spring.datasource.url=jdbc:mysql://localhost:3306/global_data_tek_onboarding?useSSL=false&serverTimezone=UTC

# JPA Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true

# JWT Configuration
# JWT secret key
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
token.secret.key=74432e706e5f86b291b696e12f33aa90d038b8c91c19eee5df5cca698ee00766
# JWT expiration is 1 hour
token.expirationms=3600000

# Servlet Configuration
spring.servlet.multipart.max-file-size=10MB

# OpenAPI Configuration
# Development and Production URLs
bezkoder.openapi.dev-url=http://localhost:8080
bezkoder.openapi.prod-url=https://bezkoder-api.com